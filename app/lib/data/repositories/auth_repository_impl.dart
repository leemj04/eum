// import 'package:eum/data/datasources/remote/auth_remote_datasource.dart';
// import 'package:eum/domain/repositories/auth_repository.dart';

// AuthRepository 인터페이스의 구현체입니다.
// Remote/Local 데이터 소스를 사용하여 실제 데이터 작업을 수행합니다.

class AuthRepositoryImpl /* implements AuthRepository */ {
  // final AuthRemoteDataSource remoteDataSource;
  // final AuthLocalDataSource localDataSource;

  // AuthRepositoryImpl({
  //   required this.remoteDataSource,
  //   required this.localDataSource,
  // });

  // @override
  // Future<void> login(String email, String password) async {
  //   // TODO: API 호출 및 예외 처리, 데이터 변환 로직 구현
  //   // final remoteData = await remoteDataSource.login(email, password);
  //   // await localDataSource.saveAuthToken(remoteData.token);
  // }
}
