import 'package:flutter/material.dart';

class RoadmapBlockWidget extends StatelessWidget {
  const RoadmapBlockWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(padding: const EdgeInsets.all(8), child: const Text('로드맵 블록'));
  }
}
